@page "/fileOpen"
@using Microsoft.AspNetCore.Http
@using System.IO;
@namespace FileManager.Pages
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@{
    string fileType = "video";

    bool fileOk = false;
    var Requset = HttpContext.Request.Query;
    fileOk = Requset.TryGetValue("File", out var file);
    //FileStream fs = new FileStream(file, FileMode.Open);
    string Fpath = @"'E://Music//4KD//DHARIA - August Diaries (by Monoir) [Official Video].mp4'";

}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>File - </title>
    <base href="~/" />
    <link href="/assets/css/bootstrap4.css" rel="stylesheet">
    <link href="/assets/css/bootstrap.min.css" rel="stylesheet">
    <link href="/assets/css/Video.css" rel="stylesheet">
    <link href="/assets/js/Video.js" rel="stylesheet">
</head>
<body>
    <main class="page lanidng-page">
        <section style="padding:10px;">
            <video controls autoplay name="media"></video>
            <br />
            <input type="file" name="file" id="fileItem" onchange="onChange()">
            <input type="submit" value="Play">
            <script type="text/javascript">
                var URL = window.URL || window.webkitURL;

               var video = document.getElementsByTagName('video')[0];
                function onChange() {
                    var files = fileItem.files;
                    var file1 = files[0];
                    console.log(file1);
                    var url1 = URL.createObjectURL(file1);
                    console.log(url1);

                    var file = new File([''], 'E://Music//4KD//DHARIA - August Diaries (by Monoir) [Official Video].mp4', { type: "video/mp4" });
                    console.log(file);
                    var url = URL.createObjectURL(file);
                    console.log(url);
                    window.open(url, "_blank");

                    video.src = url1;
                    video.load();
                    video.onloadeddata = function () {
                        video.play();
                    }
                }

                function OpeN() {

                }
            </script>
            <input type="button" value="open" onclick="OpeN()">
        </section>
    </main>
    <script src="_framework/blazor.server.js"></script>
</body>
</html>


